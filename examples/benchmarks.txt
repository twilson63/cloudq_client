# Post Test
echo '{ "job": { "klass": "Archive", "args": { "hello": "World" }}}' >> postfile

ab -n 1000 -c 100 -T application/json -p postfile http://cloudq.heroku.com/archive

# Get Test
ab -n 1000 -c 100 http://cloudq.heroku.com/archive